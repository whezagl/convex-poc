# Convex Self-Hosted Configuration
# Backend API URL for server-side operations
CONVEX_SELF_HOSTED_URL=http://localhost:3210

# Admin key for backend operations (data import, schema management)
# Generate this by running: npm run gen-admin-key
CONVEX_SELF_HOSTED_ADMIN_KEY=

# Frontend Configuration
# Backend URL for client-side code (must be prefixed with VITE_ for Vite)
VITE_CONVEX_SELF_HOSTED_URL=http://localhost:3210
