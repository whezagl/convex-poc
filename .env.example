# Convex Self-Hosted Configuration
# Copy this file to .env and fill in your actual values

CONVEX_SELF_HOSTED_ADMIN_KEY=convex-self-hosted|your-admin-key-here
CONVEX_SELF_HOSTED_URL=http://127.0.0.1:3210
CONVEX_URL=http://127.0.0.1:3210

# GLM-4.7 Configuration (Optional)
# When set, BaseAgent routes agent SDK requests to GLM's compatible endpoint
# Get your API key from: https://chat.z.ai/auth
# GLM provides an Anthropic-compatible endpoint at https://api.z.ai/api/coding/paas/v4
# These variables are optional - when not set, agents use default Anthropic models
#
# GLM_API_KEY=your-glm-api-key-here
# GLM_BASE_URL=https://api.z.ai/api/coding/paas/v4
#
# How it works:
# - BaseAgent detects GLM_API_KEY and passes ANTHROPIC_API_KEY and ANTHROPIC_BASE_URL
#   to the spawned Claude Code process via the agent SDK's env option
# - The agent SDK then routes requests to GLM instead of Anthropic
# - Model automatically defaults to 'glm-4.7' when GLM_API_KEY is set
# - See PATTERNS.md Pattern 6 for details on model-agnostic agent configuration
#
# Z.AI API Configuration (Legacy - for GLMBaseAgent)
# Get your API key from: https://platform.z.ai/
# set it in .zshrc ZAI_API_KEY=your-zai-api-key-here

# Agent Provider Selection
# Options: "claude" (default) or "glm"
# - "claude": Uses Anthropic's Claude Agent SDK
# - "glm": Uses Z.AI's GLM-4.7 model via OpenAI SDK
# When unset, defaults to "claude"
BASE_AGENT=glm
